function Assign(props){
    return (
        <div /*hidden={props.visible}*/>
            <p>
                Assign test: {props.placeholder} based on {props.map}
            </p>
        </div>
    )
}

export default Assign;